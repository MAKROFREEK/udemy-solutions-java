public class DecimalComparator{
  public static boolean areEqualByThreeDecimalPlaces(double one, double two) {
      
    int oneCast = (int) Math.pow(one);
    int twoCast = (int) Math.pow(two);
      
    if (oneTwo == twoCast) {
        return true;
    } return false;
  }
}